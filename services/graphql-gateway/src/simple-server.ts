// Temporarily disabled due to missing @apollo/server dependencies
// To restore: npm install @apollo/server express cors

/*
Simple GraphQL server is temporarily disabled due to missing dependencies.
Install the following dependencies to restore functionality:
- @apollo/server
- express
- cors
- @types/express
- @types/cors
*/

// Export a placeholder to prevent import errors
export default {};

export const startSimpleServer = () => {
  throw new Error('Simple server disabled - missing dependencies. Install @apollo/server first.');
};